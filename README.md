## 🛠 Автоматическая установка и автообновление

Теперь рекомендованный способ установки — использовать скрипт [`install.sh`](install.sh):

```bash
curl -fsSL https://raw.githubusercontent.com/ckeiituk/geo-no-russia/main/install.sh | sudo bash
```

Скрипт выполнит:
- Загрузку последней версии базы
- Настройку пути к файлу и контейнера
- Генерацию скрипта автообновления с graceful reload Xray
- Настройку systemd таймера для ежедневного обновления

**Можно кастомизировать путь и имя контейнера — всё интерактивно при запуске.**

Для ручной установки/обновления используйте:
```bash
sudo systemctl start geo-update.service
```

Остальное руководство и инструкция по интеграции c Xray/V2Ray — ниже по тексту.

---

